import { ZippyFormatDirective } from './zippy-format.directive';

describe('ZippyFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new ZippyFormatDirective();
    expect(directive).toBeTruthy();
  });
});
